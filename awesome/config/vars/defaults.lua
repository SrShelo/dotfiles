-- Here I set default programs to open or use in awesome

local defaults = {}

defaults.terminal = 'kitty'
defaults.editor = 'code'
defaults.editor_cmd = defaults.terminal .. ' -e ' .. 'vim'


return defaults